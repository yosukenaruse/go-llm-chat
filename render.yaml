services:
  - type: web
    name: go-llm-chat
    env: go
    buildCommand: go mod tidy && go build -o app .
    startCommand: ./app
    envVars:
      - key: API_KEY
        sync: false
      - key: PASSWORD
        sync: false
